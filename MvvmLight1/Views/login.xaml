<Page
    x:Class="MvvmLight1.login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MvvmLight1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding LoginViewModel, Source={StaticResource Locator}}"
    >
    
    <Grid>
            <StackPanel Orientation="Vertical" Height="300" Width="250" HorizontalAlignment="Center"  Margin="0,0,0,0" >
            <TextBox  x:Name="in_email" Header="Email" Margin="0,5,0,0" MinWidth="200" VerticalAlignment="Stretch" />
            <PasswordBox  x:Name="in_password" Header="Wachtwoord"  VerticalAlignment="Top" MinWidth="200" Padding="10,3,6,5" Margin="0,5,0,0"/>
            <Button Click="Login_Click" Content="Aanmelden" Background="Gray" Margin="0,15,0,0" MinWidth="100" VerticalAlignment="Stretch" RenderTransformOrigin="0.504,2.518"/>
               
        </StackPanel>
           
    </Grid>
</Page>
